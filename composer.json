{
  "name": "eace/passwordless_login",
  "description": "Passwordless login for REDAXO 5",
  "license": "MIT",
  "require": {
    "squizlabs/php_codesniffer": "^3.6",
    "overtrue/phplint": "^4.1"
  },
  "scripts": {
    "lint": "phplint --no-cache lib",
    "phpcs": "phpcs --extensions=php --standard=phpcs.xml lib --graceful-warnings",
    "phpcbf": "phpcbf --extensions=php --standard=phpcs.xml lib"
  }
}
